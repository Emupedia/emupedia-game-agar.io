# Server settings presets

`classic-ffa.json` is an optional partial settings preset. It intentionally omits administrator credentials, listener addresses, accepted origins, server branding, welcome messages, and chat policy.

Merge a preset into an existing `settings.json` rather than replacing the entire file. Review the bot count and anti-teaming preference before using it on a public server.
